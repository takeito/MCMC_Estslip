EST_Block_slip についてブロック運動とプレート境界のカップリングを推定未知パラメータ	ブロックのポール	ブロック間のカップリング率必要なデータ	観測データ		観測点名　経度　緯度　高さ　EW NS UD E_EW E_NS E_UD		ALPA -72.9177550 11.5279842 0 9.893 13.627 0 0.925 0.912 0		誤差成分が０の場合は使用しない　　	ブロック定義ファイル（番号_ブロック名.txt）		特定のフォルダー内（PRM.DIRBlock='./BLOCK/';）に入れる。		読み込みの順番が、ブロック番号になる。		ブロック境界の走向は若いブロック番号の順番方向に相当する。		フォーマットは 経度　緯度 の形式		ブロックを囲む必要がある。	詳細境界定義ファイル（triB_番号1_番号2.txt)		番号１、番号２はそれぞれのブロックの番号		番号１＜番号２である必要がある。		フォーマットは			経度1 緯度1 深さ1			経度2 緯度2 深さ2			経度3 緯度3 深さ3			経度1 緯度1 深さ1			>		の繰り返し（ファイルの最後には ”>”をつけない。）	ブロック境界定義ファイル（B_番号1_番号2.txt）		ブロック境界の形状定義		番号１、番号２はそれぞれのブロックの番号		番号１＜番号２である必要がある。		フォーマットは			経度 緯度 深さ			。。。。		内挿関数は			F=scatteredInterpolant(dep_blk(:,1),dep_blk(:,2),dep_blk(:,3),'natural');		メッシュグリットサイズ　int_lat=0.25;　int_lon=0.25;		緯度経度を0.25度の		[slon,slat]=meshgrid(min_lon:int_lon:max_lon,min_lat:int_lat:max_lat);		メッシュグリットを生成して 三角要素を作る。	ブロック境界領域定義ファイル（BO_番号1_番号2.txt）		ブロック境界定義ファイルの使用領域の設定		番号１、番号２はそれぞれのブロックの番号		番号１＜番号２である必要がある。		フォーマットは			経度 緯度			。。。。		で領域を囲む必要がある。		はじめに、詳細境界定義ファイル（triB_番号1_番号2.txt)を調べて	なければ、	ブロック境界定義ファイル（B_番号1_番号2.txt）	ブロック境界領域はブロック境界領域定義ファイル（BO_番号1_番号2.txt）で定義されるが、	（BO_番号1_番号2.txt）ファイルがなければ、（B_番号1_番号2.txt）の領域を全て使う。	ただし、dep_limit=150;によって、これよりも浅いところのみを選択するように設定している。　（B_番号1_番号2.txt）ファイルが無い場合には、	dep_limit_low=10;地表から深さ10kmの横ずれ断層を自動的に設定する。	ファイルを読み込んだのち、Green関数を計算する	[TRI]=GREEN_TRI(BLK,OBS);Green関数を再構築する	再構築のGreen関数は以下の通り、	d= (G.C * ( Mc .* ( G.T * ( G.B1 - G.B2 ) * Mp ) ) + G.P * Mp		d:観測データ		G.C:カップリングに起因するGreen関数（弾性体)		G.T:座標変換に対応する変換行列		G.B:ポールの推定に関するサブ断層に対するGreen関数（剛体運動）		G.P:ポールの推定に関する観測点に対するGreen関数（剛体運動）		Mc: カップリングの係数【-１～１】（未知数）		Mp: ポールの位置（未知数）この観測方程式をMCMCにて解く	解き方についてはまた今度	課題	制約条件を課した場合の確率密度関数への影響について	（おろらく小さいが。。）　